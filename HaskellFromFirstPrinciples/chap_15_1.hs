-- monoid

-- A monoid is a binary associative operation with an identity

-- class Monoid m where
--   mempty :: m                   the identity
--   mappend :: m -> m -> m        the associative operation
--   mconcat :: [m] -> m
--   mconcat = foldr mappend mempty


mappend [1,2] [4,5]
